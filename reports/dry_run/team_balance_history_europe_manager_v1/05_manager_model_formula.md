# MANAGER TRANSITION FORMULATION

$$\Delta_{	ext{manager}} = Q_{	ext{new}} - Q_{	ext{old}} + C_{	ext{tactical}} + S_{	ext{fit}} - U_{	ext{regime}} - L_{	ext{continuity}}$$

Where $L_{	ext{continuity}} = 0.0$ for retained managers and $0.03$ for new manager transitions.
