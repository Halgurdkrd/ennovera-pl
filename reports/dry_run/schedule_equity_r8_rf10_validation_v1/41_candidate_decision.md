# CANDIDATE SCIENTIFIC DECISION

- **Primary Finding:** `R8 + RF10` achieves the strongest, most consistent, and statistically validated performance across LOSO, subgroup, and calibration tests.
- **Recommendation:** Package `R8 + RF10` as `ENNOVERA_PL_R8_RF10_CHALLENGER_V1` for parallel shadow tracking alongside `ENNOVERA_PL_RECENCY_CHALLENGER_V1`.
- **Classification:** `R8_RF10_STRONG_SHADOW_CANDIDATE`.
