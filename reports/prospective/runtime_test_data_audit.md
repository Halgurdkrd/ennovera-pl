# ENNOVERA RUNTIME TEST DATA AUDIT

- **Hard Assertion:** Runtime prospective prediction pipelines are forbidden from loading files containing `tests/`, `fixtures/`, `sample/`, `demo/`, or `seed_old/`.
- **Enforcement:** Enforced in `test_prospective_validation_v1.py`.
