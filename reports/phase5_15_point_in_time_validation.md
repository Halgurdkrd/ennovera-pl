# ENNOVERA PHASE 5.15 — POINT-IN-TIME VALIDATION

- Confirmed that zero season-total aggregates or future tournament outcomes are present in `fpl_crosscomp_eligibility.parquet`.
- Features for GW $t$ are derived strictly and exclusively from historical matches flagged as `is_eligible == True`.
